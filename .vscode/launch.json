{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "java",
            "name": "Launch Test",
            "request": "launch",
            "mainClass": "UnitTest",
            "preLaunchTask": "Build Code to bin",
            "classPaths": [
                "${workspaceFolder}/bin",
                "${workspaceFolder}/class"
            ],
            "cwd": "${workspaceFolder}"
        }
    ]
}
